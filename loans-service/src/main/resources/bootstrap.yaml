spring:
  application:
    name: loans-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/dbprestabanco2
    username: postgres
    password: newpassword
  jpa:
    hibernate:
      ddl-auto: update